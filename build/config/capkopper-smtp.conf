
/**
 * capKopper SMTP module
 *
 * The following variables may be defined here, depending on your needs:
 * - capkopper_smtp:environment: It defines the current environement. The value is
 *   generally one of the following:
 *   - (dev|development): The developpement area.
 *   - (staging|preprod|preproduction): The staging server.
 *   - (prod|production|live): The production server.
 * - capkopper_smtp:recipients: A coma separated list of recipients. The email message
 *   will not be delivered to the email addresses defined in the "To" property
 *   but will be sent to the recipients defined here.
 * - capkopper_smtp:account: A extra parameter that is usually used for filtering on the
 *   SMTP gateway.
 */
$conf['capkopper_smtp:environment'] = '{{ CK_SMTP_ENVIRONMENT }}';
$conf['capkopper_smtp:recipients'] = '{{ CK_SMTP_RECIPIENTS }}';
$conf['capkopper_smtp:account'] = '{{ CK_SMTP_ACCOUNT }}';
$conf['swiftmailer_smtp_username'] = '{{ CK_SMTP_USERNAME }}';
$conf['swiftmailer_smtp_password'] = '{{ CK_SMTP_PASSWORD }}';
